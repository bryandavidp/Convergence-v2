package com.deploy21.convergence;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

/**
 * Smoke test local que confirma que el runner JUnit del proyecto está operativo.
 */
public class ExampleUnitTest {

    @Test
    public void junitRunnerIsAvailable() {
        assertEquals(4, 2 + 2);
    }
}
